/-  *proof-stream
|%
+$  proof         [stream-state heights=(list @)]
+$  prove-result
  ::  only used by proxy and prover app
  (each =proof err=prove-err)
+$  prove-err
  $%  [%too-big heights=(list @)]
  ==
--