|%
+$  proxy-request
  $%  [%submit-to-proxy [s=* f=*] n=@ ver=@]  :: n is random value, ver is the interval revision of the prover that the thread declares compatibility with
  ==
+$  proxy-command
  $%  [%set-hive hive=(set ship)]  :: replace hive
  ==
::
+$  proxy-update
  $%  [%proxy who=ship]
  ==
--
